---
description: إدارة Docker وحاويات. استخدم: /docker [build/push/run/compose] [خيارات]
agent: devops-agent
---

نفذ مهمة Docker التالية: $ARGUMENTS

مثال: `/docker build . -t myapp:latest` أو `/docker compose up -d`
